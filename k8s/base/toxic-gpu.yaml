apiVersion: apps/v1
kind: Deployment
metadata:
  name: toxic-gpu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toxic-gpu
  template:
    metadata:
      labels:
        app: toxic-gpu
    spec:
      initContainers:
        - name: model-downloader
          image: python:3.11-slim
          command:
            - /bin/sh
            - -c
            - |
              pip install --no-cache-dir transformers torch && \
              python -c "from transformers import pipeline; pipeline('text-classification', model='martin-ha/toxic-comment-model', cache_dir='/models')"
          volumeMounts:
            - name: model-cache
              mountPath: /models
      containers:
        - name: toxic-service
          image: toxic-service:latest
          imagePullPolicy: Never
          env:
            - name: SERVICE_TYPE
              value: "gpu"
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "1000m"
              memory: "1Gi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
          volumeMounts:
            - name: model-cache
              mountPath: /models
      nodeSelector:
        accelerator: nvidia-gpu
      volumes:
        - name: model-cache
          persistentVolumeClaim:
            claimName: toxic-model-cache
---
apiVersion: v1
kind: Service
metadata:
  name: toxic-gpu-service
spec:
  selector:
    app: toxic-gpu
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
