[project]
name = "orchestrator-service"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "prometheus-fastapi-instrumentator",
    "pydantic>=2.5.0",
]
